name: Extract Changes

on:
  push:
    paths:
      - '**.txt'  # or specify specific file types you're interested in

jobs:
  extract:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Important! Full history needed for proper diffs

      - name: Extract Changes
        run: |
          echo "🔍 Extracting added lines from modified files..."
          git diff HEAD^ HEAD > diff.txt
          echo "📄 Diff output saved to diff.txt"
          echo "📥 Extracting Added/Deleted/Changed lines..."
          grep '^+' diff.txt | grep -v '^+++' | sed 's/^+//' > added_lines.txt
          echo "✅ Added/Deleted/Changed lines extracted:"
          cat added_lines.txt

      - name: Call BTP CI IFlow
        run: |
          curl -X GET https://589d90eftrial.it-cpitrial06-rt.cfapps.us10-001.hana.ondemand.com/http/SyncArtifacts/GITHUB \
               -H "Content-Type: application/text" \
               -H "Authorization: Bearer ${{ secrets.API_KEY }}" \
               -d 'POCArtifactsBackup,Iflow,BTP_IS_Artifacts_Backup_Test,1.0.3,01.07.2025,abhay.q.kumar@accenture.com
                   POCArtifactsBackup,Iflow,POC_ArtifactsBackUp,1.0.4,26.06.2025,abhay.q.kumar@accenture.com
                   POCArtifactsBackup,Iflow,POC_ArtifactsBackUp25,1.0.3,25.06.2025,abhay.q.kumar@accenture.com'
